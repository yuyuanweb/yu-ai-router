[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "yu-ai-router-sdk"
version = "0.1.0"
description = "Python SDK for Yu AI Router"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27.0"]

[tool.setuptools]
packages = ["yu_ai_router_sdk"]
